<Page
    x:Class="RestaurantManager.UniversalWindows.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RestaurantManager.UniversalWindows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.4*"/>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>
        <TextBlock  x:Name="txtTitle" Text="RestaurantManager" FontSize="50" HorizontalAlignment="Center"  VerticalAlignment="Bottom" Grid.ColumnSpan="2"/>
        <TextBlock  x:Name="txtSubtitle" Text="Internal test version" HorizontalAlignment="Center"  VerticalAlignment="Bottom" Grid.Row="1" Grid.ColumnSpan="2" />
        <TextBlock x:Name="txtOrders" Text="** Go to orders view **" VerticalAlignment="Center" Grid.Row="2" HorizontalAlignment="Center"/>
        <TextBlock x:Name="txtSubmit" Text="** Submit order **" VerticalAlignment="Center" Grid.Row="2" HorizontalAlignment="Center" Grid.Column="1" />
    </Grid>
</Page>
